{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd reading_app_backend && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn reading_app_backend.wsgi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
